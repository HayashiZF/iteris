interface:
  display_name: "Iteris Math"
  short_description: "Run the portable Iteris operator/runtime loop on a repo-local mathematical workspace."
  default_prompt: "Use $iteris-math to supervise this Iteris-style workspace through repo-local state, portable helper scripts, and contracted subagents."
